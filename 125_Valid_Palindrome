class Solution {
    public boolean isPalindrome(String s) {
		String refined = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
		String reverse = new StringBuilder(refined).reverse().toString();
		
		return refined.equals(reverse);
        
    }
}
